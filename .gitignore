# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env — qualquer arquivo .env fica fora do Git (inclui .env.secrets,
# usado em `supabase secrets set --env-file .env.secrets`).
.env
.env.*
!.env.example

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Supabase CLI local
supabase/.temp/
supabase/.branches/

# Build artifacts (gerados por scripts/optimize-images.mjs).
# Comente a linha abaixo se quiser comitar as variantes WebP e
# evitar build step em produção.
# images/optimized/

# Playwright (Onda 12)
test-results/
playwright-report/

# Pasta local de inputs/seeds (nao versionar)
_ervatorio-inputs/
supabase/fichas-ancora-import.json.bak-*
